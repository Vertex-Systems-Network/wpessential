# Bank Status

Canonical Master Options Bank discovery currently has three seeded surfaces: CPT, Taxonomy, and Fields / Field Groups. The remaining canonical surfaces are unseeded unless `../options-bank-progress.json` says otherwise.

`BANK_SURFACE_SEEDED` is discovery truth only. It does not imply native audit, market audit, Bank review, implementation readiness, runtime certification, or shipped product parity.

Use `../options-bank-progress.json` as the canonical machine-readable count and lifecycle-status source.
